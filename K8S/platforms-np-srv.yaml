apiVersion: v1
kind: Service
metadata:
  name: platformnpservice-srv
spec:
  type: NodePort
  selector:
    app: dofushdvservice
  ports:
    - name : dofushdvservice
      protocol : TCP
      port : 80
      targetPort: 80